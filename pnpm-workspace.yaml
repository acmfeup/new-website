packages:
  - "apps/*"
  - "packages/*"

# Both need their native postinstall: sharp is Next.js' image optimiser,
# esbuild is how drizzle-kit loads drizzle.config.ts.
allowBuilds:
  esbuild: true
  sharp: true
